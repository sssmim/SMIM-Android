<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="org.techtown.smim">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Smim"
        android:usesCleartextTraffic="true">
        <activity android:name=".ui.home.test"></activity>
        <activity android:name=".ui.dashboard.YoutubePlan" />
        <activity android:name=".ui.dashboard.ExercisePlan" />
        <activity android:name=".ui.notifications.CustomExerciseMerge" />
        <activity android:name=".ui.dashboard.MakeGroup" />
        <activity android:name=".ui.dashboard.DashboardTrial" />
        <activity android:name=".ui.notifications.NotificationsTrial" />
        <activity android:name=".ui.notifications.ExerciseTimer" />
        <activity android:name=".ui.dashboard.GroupExercisePlay" />
        <activity android:name=".ui.notifications.ExerciseInfo" />

        <activity
            android:name=".MainActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>